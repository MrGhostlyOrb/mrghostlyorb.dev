version: "3.9"

services:
  web:
    container_name: mrghostlyorb-dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - nginxproxymanager
networks:
  nginxproxymanager:
    external: true
